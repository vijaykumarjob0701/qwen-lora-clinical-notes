# Pinned-ish stack for the LoRA SFT tutorial.
# Install a CUDA build of torch that matches your GPU from https://pytorch.org
# before this file if you plan to train (CPU-only is fine for --preview).

torch>=2.3.0,<2.7
transformers>=4.44.0,<4.50
peft>=0.12.0,<0.15
trl>=0.11.0,<0.14
datasets>=2.21.0,<3.2
accelerate>=0.34.0,<1.3
safetensors>=0.4.5

# Optional 4-bit / 8-bit loading (skip if you have no NVIDIA GPU)
bitsandbytes>=0.43.0,<0.45

# Logging / eval helpers
tqdm>=4.66.0

# GIF diagram generation (docs/gifs)
pillow>=10.4.0
imageio>=2.35.0

# Tutorial smoke tests (CPU)
pytest>=8.0.0
